@model EntityLayer.Concrete.Sector
@{
    ViewBag.Title = "AddSector";
    Layout = "~/Views/Shared/_JobLayout.cshtml";
}

<h2>Sektör Ekleme Sayfası</h2>
<br />
@using (Html.BeginForm("AddSector", "Sector", FormMethod.Post))
{
<div class="form-group">
    @Html.HiddenFor(x => x.SectorID)
    @Html.Label("İsim")
    @Html.TextBoxFor(x => x.Name, new { @class = "form-control" })
    <br />
    @Html.Label("Durum")
    @Html.TextBoxFor(x => x.Status, new { @class = "form-control" })
</div>
    <br />
    <button class="btn btn-outline-info">Kaydet</button>

}

